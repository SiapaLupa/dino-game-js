// used when running the game (always change)

export default abstract class GameVariables {
  protected _isGameOver: boolean = false;
  protected _currentScores: number = 0;
  protected _highscores: number = 0;
}
